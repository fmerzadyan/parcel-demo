{
  "name": "parcel-demo",
  "version": "1.0.0",
  "description": "A workspace to demonstrate the capabilites of parcel - which is an alternative to wepback - but is better since it requires less configuration.",
  "main": "index.js",
  "scripts": {
    "dev": "parcel src/index.html",
    "build": "parcel build src/index.html --out-dir production"
  },
  "author": "fmerzadyan",
  "license": "MIT",
  "dependencies": {
    "@babel/plugin-proposal-class-properties": "^7.1.0",
    "parcel-bundler": "^1.10.3",
    "sass": "^1.14.3"
  },
  "devDependencies": {
    "@babel/core": "^7.1.2"
  }
}
